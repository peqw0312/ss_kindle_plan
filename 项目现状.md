# 项目现状说明

> 写给不看代码的人（也就是你）。日期：2026-09-22
> 想照着操作，看 [`guide/`](guide/00-操作总览.md)；想知道怎么搭，看 [`README.md`](README.md)。
> **这份只回答三个问题：现在是什么状态、什么已经确定、什么还悬着。**

---

## 一、这个项目是什么

一台 2015 年的 Kindle Paperwhite 3，挂在墙上/放在桌上，当**每日信息屏**用。

屏幕上显示的是一张 **1072×1448 的灰度图片**：大号日期 + 农历黄历 + 天气 + 四天预报 + 三个指数行情。它不是 App，不滚动、不点击，就是一张每天重画几次的图。

三条不可动摇的约束（你定的）：

| 约束 | 含义 |
|---|---|
| **零成本** | 不付费、不绑卡、不实名、不注册需要这些信息的服务 |
| **数据来自云端，不来自电脑** | 电脑必须可以关机 |
| **不装额外软件** | Kindle 端只有 shell 脚本 + 越狱自带的小工具 |

---

## 二、整条链路（图是怎么到屏幕上的）

```
   ① 出图                ② 存图                 ③ 显示
┌──────────────┐    ┌──────────────┐      ┌──────────────┐
│ GitHub Actions│    │ 本仓库 docs/  │      │ Kindle PW3   │
│ 跑 generate.py│ →  │ dashboard.png │ →   │ 下载 → eips 刷 │
│ dashboard/   │    │ （raw 地址）   │      │ 屏 → 睡觉     │
│ render.py    │    │              │      │              │
└──────────────┘    └──────────────┘      └──────────────┘
   抓天气/行情/农历        一天提交四次            一天来拉四次
   画成一张灰度 png
```

**① 出图**：`dashboard/` 那套 Python。抓天气（现在走 Open-Meteo，因为和风天气的 key 没配）、抓行情（腾讯/新浪），农历黄历本地算（不联网），然后用 Pillow 画成一张图。**屏幕上所有东西——包括字体和天气图标——都是这张图里画死的像素。**
跑它的是 GitHub Actions（`.github/workflows/build.yml`），**不是你的电脑**。

**② 存图**：Actions 把生成的 `docs/dashboard.png` 提交回仓库，Kindle 用
`raw.githubusercontent.com/...` 这个公开地址去拿。没有服务器，没有进程要养。

**③ 显示**：Kindle 只负责下载 + 刷屏幕 + 睡觉。它不生成任何东西。
"几点来拉"也归它自己管（`config.sh` 的 `FETCH_EVERY_HOURS` / `FETCH_ALIGN_MINUTE` /
`QUIET_START`~`QUIET_END`）—— GitHub 是静态文件，发不了自定义头，
所以以前那套"云端告诉设备几点换新图"搬到了设备上。

> ⚠️ 关键认知：**改了 `dashboard/` 里的东西，必须 commit + push 到 GitHub 才会生效。**
> 本地跑 `generate.py` 出的那张只有你自己看得到，Kindle 看不到。
> push 之后也不是立刻变：Actions 要跑两三分钟，raw 的 CDN 还有约 5 分钟缓存。

---

## 三、当前配置一览

| 项 | 现在的值 | 在哪改 |
|---|---|---|
| 地点显示 | 杭州 · 临平区 | `dashboard/config.yaml` |
| 版式引擎 | `poster`（「帖/巨」这一系，居中、宋体大字、留白优先） | `config.yaml` → `style.layout` |
| 字号预设 | `大字`（只对 `bands` 生效，poster 下不起作用） | `config.yaml` → `style.preset` |
| 天气图标 | **E 套「柔雾 · 灰实心」**：整块深灰、四瓣云、雨是水滴、太阳是一圈点 | `dashboard/aiinfo/render.py` 的 `ICON_*` 常量 |
| 时钟 | **关掉**（`clock.mode: "off"`） | `config.yaml` + Kindle `config.sh` **必须一起改** |
| 电量角标 | 设备本机贴精灵图（`BATTERY_MODE=local`） | `config.sh` |
| 预报天数 | 4 天（含今天） | `config.yaml` → `weather.days` |
| 今日速览（AI 新闻） | **关闭** | `config.yaml` → `digest.enabled` |
| 行情 | 开，3 个指数：纳指100 / 标普500 / 上证指数 | `config.yaml` → `quotes` |
| 出图时刻 | 云端每小时一次；设备每小时**第 10 分**来取，凌晨 1–7 点完全不联网 | 云端 `.github/workflows/build.yml` 的 cron（UTC）+ 设备 `config.sh` 的 `FETCH_*` / `QUIET_*` |
| 图片地址 | 三条出口按顺序试：**Pages** → jsDelivr → raw（都在 `screen` 分支上） | Kindle 端 `config.sh` 的 `DASHBOARD_URLS` |

### 关于「时钟必须两边一起改」

这是个静默故障点，单独说一下：

- 电脑 `config.yaml` 的 `clock.mode` 和 Kindle `config.sh` 的 `CLOCK_MODE` **必须一致**
- 不一致的后果：右上角永远空一块（一边留白等贴图，另一边根本不贴）
- 而且 YAML 里 `off` **必须带引号**写成 `"off"`，裸写会被解析成布尔 `False`，屏幕上就会画一个几小时前的假时间，还不报错

---

## 四、Kindle 端是怎么运作的

装的东西全在 U 盘里，没有安装步骤：

```
documents/scriptlets/*.sh      ← 书库里能点的 7 个"书本"图标
extensions/aistatus/
  ├── config.sh                ← 所有可调参数都在这里
  ├── bin/aistatus.sh          ← 主循环：下载、刷图、休眠、贴电量
  ├── bin/start.sh stop.sh refresh.sh log.sh info.sh clock_check.sh
  ├── battery/                 ← 11 张电量角标 + battery.conf（坐标）
  └── clock/                   ← 1440 张时钟精灵图（现在用不上，时钟关了）
```

**退出信息屏有四条路**（都必须故意的动作）：

1. **快速连点屏幕三下**（每两下 ≤8 秒）→ 新增的，见第六节
2. 点「停止信息屏」—— 但省电模式下书库是关着的，点不到
3. 插 USB，在 `extensions/aistatus/` 里建一个空文件叫 `.stop`（最慢 10 分钟生效）
4. **长按电源 40 秒强制重启** —— 永远有效的保底

---

## 五、2026-09-22 解决了什么（全部有实测证据）

### 1. 掉电元凶 —— 找到三层，前两层已确认修好

这台设备一晚掉 60% 电。真正的原因不是一个，是叠了三层：

| 层 | 问题 | 状态 |
|---|---|---|
| ① | 代码找的是**老内核**的唤醒节点 `wakeup_enable`，这台 PW3 上根本没有，只有 `wakealarm`。找不到就悄悄退化成普通 sleep → **CPU 整夜醒着** | ✅ 已修，实测从"睡 1 秒"变成"睡 121 秒" |
| ② | ~~`WIFI_SLEEP=0` WiFi 常开导致 `echo mem` 睡不进去~~ | ❌ **这个判断是错的**。改成 1 之后实测反而更差（第一次 121s、随后 1s 弹回空转），而保持 0 时是 181/178/175s 精准休眠。真正起作用的是第 ①③ 层。已退回 0，并在 `config.sh` 里写了别再改回去 |
| ③ | 人为唤醒之后，事件还没排空就 `echo mem` → 内核立刻 abort（表现是 1 秒弹回），循环于是**空转烧 CPU** | 🔶 已修（补上 `/sys/power/wakeup_count` 协议 + 连蹦三次就改普通 sleep 熬时间），**还没上机验证** |

### 2. 「碰一下就回主界面」—— 根因很荒唐

以前用的命令是 `/etc/init.d/framework stop`。但这台机器上 **`/etc/init.d` 是个空目录**，那条命令返回 **127 = 命令不存在**。

**也就是说：Kindle 的界面从头到尾就没被停过。** 你碰屏幕，它当然重画主页面；系统时钟当然盖在你画面上。不是"对抗不了系统刷新"，是那一枪根本没打响。

改成 upstart 的 `initctl stop framework pillow statusbar webreader` 之后，实测：

```
界面任务已停 · 进程数 106 → 94 · 仍在跑：无
```

**四个任务全停，我们自己的进程还活着。** 顺带发现 `statusbar`（顶部状态栏）是独立任务 —— 只停 framework 管不了那个时钟。

### 3. 屏幕 79 分钟不刷新 —— 一个 shell 兼容性 bug

`hour=$((10#$hour))` 这种带基数的写法 busybox 的 ash 不认 → 函数输出空 → 变量变空串 → 主循环那句 `[ 现在 -ge "" ]` **直接报错返回"假"** → 永远判定"还没到点"。

日志铁证：刷新一次之后，**79 分钟一次都没刷新**。

这也解释了"电量显示不实时"——**不是设计问题，是屏幕根本没在刷新**。

现在算不出时刻会兜底成 10 分钟，并且**在日志里喊出来**（静默冻屏是最难查的故障）。

### 4. 其他

- **天气图标整套换成 E 套**（从六套候选里选的），旧的"细描边+浅灰云体"代码已删
- **地址改成「杭州 · 临平区」**
- **版面模块化**：四块可以任意顺序重排，`config.yaml` 里改
- **调参台** `ui_studio.py`：浏览器里拖模块、拉字号，每次改动都用真渲染器重出真图
- **图标选型页** `icon_sets.py`：六套完整图标 + 真实尺寸对照
- **注销了 Windows 计划任务 `KindleAIScreen`** —— 它每天弹 4 次黑窗口，而且只往本地写文件、不上传任何东西，纯属白跑
- **`refresh.sh`（「测试刷新一次」）补上电量角标那一步**，并改成只试主地址——以前主备都试，退到备用地址时会拿回一张**不同版式**的图，于是"我改了怎么没生效"白查半天
- **清掉过时的 KUAL 提示**：你设备上没装 KUAL，屏幕上却写着"在 KUAL 里点"

### 5. 改走 GitHub，电脑彻底退出（2026-09-24）

- **新增 `.github/workflows/build.yml`**：每小时跑一次 `generate.py`，把图**覆盖**到专用分支 `screen`（主分支历史不涨）
- **删掉三个"电脑当服务器"的东西**：`dashboard/app.py`（自建出图服务）、`dashboard/serve.py`（局域网静态发图）、`dashboard/tools/install_task.py`（注册那个弹黑窗口的计划任务）。连带把 `check_cloud.py` 从"验自建服务"改成"把设备那三条出口全试一遍"
- **取图节奏搬进设备并合并成一个规则**：`config.sh` 的 `FETCH_EVERY_HOURS` + `FETCH_ALIGN_MINUTE` + `QUIET_START`/`QUIET_END`，纯整数运算（不碰 `date -d`，busybox 上它解析失败是静默的）。原来六个管同一件事的旋钮（`REFRESH_AT` / `REFRESH_LAG` / `UPDATE_INTERVAL` / `NIGHT_INTERVAL` / `ACTIVE_START` / `ACTIVE_END`）全删
- **`config.yaml` 里的 `cloud:` 整段删掉**：那曾是 app.py 读的配置，现在没人读 —— 留着就是个假开关，改完一点反应没有
- **顺手抓到两个真 bug**：
  - 「带」版（c1）画气象预警时把**字典当字符串**喂给排版函数 → 有预警的那天直接崩，Actions 出图失败、屏幕悄悄停在旧图上。已修，`layout_check.py` 的预警场景现在跑得通
  - `layout_check.py` 的"这版式怎么量"判断里**漏了 c1**，于是 c1 一直拿 bands 的尺寸报 ✅ —— 假通过比不查更糟。已修，修完立刻看出：平时余量 162px，两条预警同时出现时 66px（安全线 40）

---

## 六、还悬着的事（按重要性排）

| # | 事项 | 说明 |
|---|---|---|
| 1 | **设备上那份脚本还是旧的** | 仓库里 `config.sh` 已经是 GitHub 地址 + 每小时第 10 分取图 + 01:00–07:00 静音，Kindle 上跑的不一定是这版。要确认得拔线→长按 40 秒重启→「启动信息屏」→插回来读日志 |
| 2 | **没有干净的耗电基线** | 休眠那条链已经修到"真睡 600 秒"，但"拔线能撑几天"只有一个粗略观感。做法：记下百分比，拔线 12 小时后再看 |
| 3 | **GitHub 的整点任务实际约 3 小时一次** | 实测触发时刻 22:31 / 02:45 / 05:54 / 08:10 / 13:37。`cron: 0 * * * *` 写的是每小时，公开仓库排队就是这么久 —— 屏上数字最多旧 3 小时，不是 1 小时 |
| 4 | **第 3 层休眠修复未验证** | `wakeup_count` 那套已推到设备，要拔线跑一轮看"实际睡了"能不能接近 600 秒 |
| 5 | **三下退出只验到 1/3** | 判据活了，但没连够过三下。而且有个根本疑问：**内核唤醒源清单里没有触摸屏** —— 如果设备真睡下去，碰屏幕可能根本叫不醒它，那这个出口只能靠电源键 |
| 6 | **SSH 没验过** | 设备上 `sshd` 在跑。通了以后就不用反复插拔线了 |

---

## 七、日常操作速查

**改设计 / 改数据 / 改时刻** → 都在电脑这边：

```
改 dashboard/aiinfo/render.py 或 config.yaml
  ↓
.\.venv\Scripts\python.exe dashboard\tools\layout_check.py   ← 自检：五个场景真渲染一遍量余量
  ↓
.\.venv\Scripts\python.exe dashboard\generate.py             ← 本机出一张，先看效果
  ↓
git add -A; git commit -m "…"; git push                     ← 到这一步 Kindle 才可能看到
  ↓
（等两三分钟 Actions 跑完，再等约 5 分钟 CDN 缓存）
  ↓
.\.venv\Scripts\python.exe dashboard\tools\check_cloud.py    ← 验那个地址真拿到图了
  ↓
书库里点「⟳ 测试刷新一次」                            ← 真去下载一次并打印 7 步诊断
```

> 注意：**碰屏幕不会去下载新图**，它只把缓存里那张重新贴回屏幕（因为原生界面可能把它擦掉了）。
> 想现在就换上新图，只有两条路：等时刻表下一个点，或者点「测试刷新一次」。

**想立刻看效果不想等定时** → 去仓库 Actions 页点 "Run workflow" 手动跑一次。

**改刷新时刻** → 两边节奏要配套：云端是 `.github/workflows/build.yml` 的 cron（UTC），
设备是 `kindle/extensions/aistatus/config.sh` 的 `FETCH_EVERY_HOURS` / `FETCH_ALIGN_MINUTE` /
`QUIET_START`~`QUIET_END`。设备对齐到第 10 分是有原因的：Actions 整点起跑、跑完要两三分钟，
前面还有一层 CDN 缓存，卡整点去取必然拿到上一张。

**改省电行为 / 改退出方式** → 在 `kindle/extensions/aistatus/config.sh`，改完要**拷进 Kindle**。

**看设备到底怎么了** → 插上 USB，读 `D:\extensions\aistatus\aistatus.log`；
现场信息在 `system_probe.txt`（每次启动重写，含内核 dmesg 的休眠记录）。

**屏幕卡住没反应** → 长按电源 40 秒。永远有效，弄不坏。

---

## 八、目录结构

```
.github/workflows/build.yml      定时出图 = 整条链路的心脏
dashboard/
  ├── generate.py       本机出一次图，写进 docs/
  ├── config.yaml       所有可调项
  ├── aiinfo/           渲染器、数据源、农历、字体、配置
  └── tools/            自检和调试工具（见下）
kindle/extensions/aistatus/      Kindle 端全部脚本 + 精灵图
kindle/scriptlets/             书库里那 7 个可点图标
guide/                         操作文档（00 总览 → 05 托管选型）
docs/                          出图结果 dashboard.png / index.html / debug.json
.workbuddy/
  ├── memory/MEMORY.md  ★ 架构约定文档（改代码前先读，改完要更新）
  ├── _studio/          调参台输出、图标选型页
  └── _directions/      设计变体对照
```

### 工具都是干什么的

| 工具 | 干什么 | 要联网吗 |
|---|---|---|
| `layout_check.py` | **改完代码必跑**：五个场景真渲染一遍，量纵向余量 + 精灵图几何同不同步 | 要 |
| `check_cloud.py` | 验 Kindle 真正拉的那个地址：是不是 PNG、规格、多久没更新（404=没 push） | 要 |
| `probe_sources.py` | 数据源自检：各家天气/行情通不通、慢不慢 | 要 |
| `icon_sets.py` | 天气图标选型页，六套完整对照（E 套就是这么选出来的） | 否 |
| `icon_sheet.py` | 把图标单独放大成一张图看细节 | 否 |
| `make_battery_assets.py` | 生成 11 张电量角标 + battery.conf 坐标 | 否 |
| `make_clock_assets.py` | 生成 1440 张时钟精灵图（`clock.mode=off` 期间用不上） | 否 |
| `verify_lunar.py` | 农历和第三方库对拍 | 否 |
| `ui_studio.py` | 调参台（浏览器拖模块）。**只对 bands 版式有用，准备砍** | 要 |
| `design_directions.py` | 上一轮设计选型用的，1500 行。**候选清理对象** | 否 |
| `refresh.sh`（设备端） | 走一遍完整链路并打印 7 步诊断 | 要 |
| `log.sh` / `info.sh` | 看日志 / 看设备信息 | 否 |

---

## 九、两条最重要的经验（别再犯）

1. **"看起来在工作"和"真的在工作"是两回事。**
   今天查出来的三个大 bug，全都是这一类：命令返回 127 但日志写着"已停止"、变量算成空串但循环只是不刷新、找不到休眠节点就悄悄退化成不睡。**修法是让每一步都留下可核对的证据**（返回码、进程数、实测秒数），而不是加更多判断。

2. **别照别的机型/别的世代的写法抄。**
   `wakeup_enable` 是老内核的、`/etc/init.d/framework` 是别的机型的、`10#` 是 busybox 不认的。这台设备的答案只能问它自己 —— 所以才有 `system_probe.txt` 这个东西。

---

## 十、致谢与来源

- [pascalw/kindle-dash](https://github.com/pascalw/kindle-dash) —— RTC 闹钟 + `echo mem` 强制休眠的思路
- [loehnertj/kindle_dashboard_viewer](https://github.com/loehnertj/kindle_dashboard_viewer) —— WiFi 状态查询、`deferSuspend` 的失败经验
- [Kindle Modding](https://www.kindlemodding.org/) —— 越狱、hotfix、scriptlet 的事实来源
- 天气图标 E 套的云形取自 [Lucide](https://lucide.dev/)（ISC 许可）的切线构造思路，实现是重写的

# 调试产物：里面有你的城市坐标和自选股清单，只是排查用的，别当源码提交
docs/debug.json

# 本地临时文件
*.log
.DS_Store
__pycache__/
*.pyc
dashboard/config.local.yaml

# 验证过程中的临时文件
.workbuddy/_*

# 发布应用留下的本地标记（里面是内部 id，留在本地就行，别提交）
.wbapp_*.genie

# 从官方 release 下下来的第三方固件包（3.5MB），不该进仓库；
# 需要时按 guide/01 里的地址重新下载即可
给Kindle/

# 里面是和 .wbapp_*.genie 同一个内部 id，按上面那条同样的理由不提交
.workbuddy/applications.yaml

# 项目自己的 Python 环境（几百万个文件，绝不能进仓库）
.venv/
venv/

# 时钟精灵图：1441 个文件、约 700KB，全是生成物。
# 需要时在电脑上跑一次就重做出来：
#     python dashboard/tools/make_clock_assets.py
# 而且现在 clock.mode=off 根本不贴钟，这批图压根用不上。
# 真要拷给 Kindle 也还是从本地目录拷 —— 所以留在磁盘上，但不进版本库。
kindle/extensions/aistatus/clock/

# .workbuddy 下散落的截图（之前 live.png 就是这么混进版本库的）
.workbuddy/*.png

# 出图结果不再进主分支历史：Actions 每次把它**覆盖**到专用分支 screen，
# 主分支这张只是本机预览（历史上那几张是改架构之前留下的）。
docs/dashboard.png
